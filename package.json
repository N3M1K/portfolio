{
  "name": "new-portfolio",
  "version": "1.0.50",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "dev:client": "bunx --bun vite dev",
    "build": "bunx --bun vite build",
    "preview": "bun run build && bun run dev"
  },
  "dependencies": {
    "@elysiajs/static": "latest",
    "elysia": "latest",
    "htmx.org": "^2.0.8",
    "svelte": "latest"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "latest",
    "bun-types": "latest",
    "vite": "latest"
  },
  "module": "src/index.js"
}
